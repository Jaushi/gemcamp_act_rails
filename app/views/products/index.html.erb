<h1>All Products</h1>

<ul>
    <% @products.each do |product| %>
        <li>
            <strong><%= link_to product.name, product %></strong> - 
            <%= product.description %> - 
            $<%= product.price %>
        </li>
    <% end %>
</ul>
